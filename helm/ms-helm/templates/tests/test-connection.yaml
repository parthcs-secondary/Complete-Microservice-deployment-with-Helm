apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-helm.fullname" . }}-test-connection"
  labels:
    {{- include "ms-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
